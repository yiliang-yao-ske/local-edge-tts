# Local Edge TTS 插件新功能指南

## 概述

本插件基于参考项目 `obsidian-edge-tts` 实现了两个主要的新功能：

1. **右键菜单语音朗读功能**
2. **内嵌音频进度条功能**

## 功能详情

### 1. 右键菜单语音朗读功能

#### 功能描述
在编辑器中右键点击时，菜单中会出现"语音朗读"选项，可以快速启动TTS功能。

#### 使用方法
1. 在编辑器中选中要朗读的文本（可选）
2. 右键点击打开上下文菜单
3. 选择"语音朗读"选项
4. 如果有选中文本，将朗读选中内容；否则朗读整个文档

#### 设置选项
- 在插件设置中可以找到"右键菜单功能"部分
- 使用"启用右键菜单语音朗读"开关来控制此功能的开启/关闭

### 2. 内嵌音频进度条功能

#### 功能描述
允许在编辑器的当前光标位置插入一个音频播放进度条，提供完整的音频控制功能。

#### 使用方法
1. 将光标放置在想要插入进度条的位置
2. 右键点击打开上下文菜单
3. 选择"插入音频进度条"选项
4. 进度条将在当前位置插入一个新行

#### 进度条功能
- **播放/暂停按钮** (▶/⏸): 控制音频播放状态
- **停止按钮** (⏹): 停止播放并重置进度
- **进度条**: 显示播放进度，可拖拽调整播放位置
- **时间显示**: 显示当前时间/总时长
- **删除按钮** (✕): 移除进度条组件

#### 设置选项
- 在插件设置中的"右键菜单功能"部分
- 使用"启用内嵌音频进度条"开关来控制此功能的开启/关闭

## 技术实现

### 文件结构
```
├── main.ts                                 # 主插件文件，添加了右键菜单和进度条功能
├── src/
│   └── modules/
│       ├── settings.ts                     # 设置界面，添加了新功能的开关
│       └── inline-progress-bar.ts          # 内嵌进度条组件实现
└── styles.css                              # 样式文件，添加了进度条样式
```

### 核心组件

#### InlineProgressBar 类
负责创建和管理内嵌进度条的所有功能：
- 创建DOM元素和样式
- 处理用户交互（播放、暂停、拖拽等）
- 管理音频状态和进度更新
- 提供回调接口与主插件通信

#### 右键菜单集成
通过 Obsidian 的 `editor-menu` 事件实现：
- 根据设置动态添加菜单项
- 智能处理选中文本和整个文档的朗读
- 集成进度条插入功能

### 样式设计

进度条组件采用了与 Obsidian 主题一致的设计语言：
- 使用 CSS 变量适配明暗主题
- 响应式设计支持移动端
- 平滑的交互动画和悬停效果
- 符合 Obsidian 设计规范的颜色和间距

## 设置说明

在 Obsidian 设置 → 社区插件 → Local Edge TTS 中：

1. **启用右键菜单语音朗读**
   - 默认：启用
   - 控制右键菜单中是否显示语音朗读选项

2. **启用内嵌音频进度条**
   - 默认：启用
   - 控制右键菜单中是否显示插入进度条选项

## 兼容性

- 兼容 Obsidian 桌面版和移动版
- 支持明暗主题自动切换
- 响应式设计适配不同屏幕尺寸

## 注意事项

1. 进度条组件会在文档中插入HTML注释作为占位符
2. 音频文件会在内存中临时创建，播放结束后自动清理
3. 同时只能有一个活跃的进度条组件
4. 在移动设备上，某些音频控制可能受系统限制

